//! yuuka-tools — `ToolProvider` 中央レジストリ（Native / MCP=rmcp / WASM=Extism）。
//!
//! Phase 0 stub。実装は Phase 2（`docs/rust-rewrite/parts/05-plugins-types.md`）。
//! `Tool` / `ToolProvider` / `ToolContext` / `ToolOutcome` 契約は `yuuka-core` に凍結済み。
//! Phase 0 決定: Native+MCP 先行、WASM(Extism) は trait 境界越しに後続フェーズで差し込む。
