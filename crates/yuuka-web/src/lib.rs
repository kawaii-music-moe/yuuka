//! yuuka-web — axum Web 層（ルート・認可 extractor・静的配信・`/ws/chat`）。
//!
//! Phase 0 stub。実装は Phase 1（`docs/rust-rewrite/parts/03-web-db.md`）。
//! 契約（`WebError` / `Envelope` / 認可型）は `yuuka-core` / `yuuka-types` に凍結済み。
